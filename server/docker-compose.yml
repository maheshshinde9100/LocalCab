version: '3.8'

services:
  localcab-backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: localcab-backend
    ports:
      - "8080:8080"
    env_file:
      - .env
    restart: unless-stopped
